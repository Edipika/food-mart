import { createSlice } from "@reduxjs/toolkit";

const categorySlice = createSlice({
    name:'category',
    initialState:{
        isEditing: false,
        selectedCategory: null,
        isSuccess: false,
    },
    reducers:{
        
    }
})
